Tool for cleaning up a folder of camera photos. After deleting the JPGs run `delnef.bat` in the respective folder to clean up the remaining files. 

You can either copy both files wherever you need them or add them to PATH to run from anywhere.

Using `recycle` from the [CmdUtils](http://www.maddogsw.com/cmdutils/) collection.