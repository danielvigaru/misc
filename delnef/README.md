Tool for cleaning up a folder of camera photos. After deleting the JPG run this in the respective folder to clean up the remaining files.

Using `recycle` from the [CmdUtils](http://www.maddogsw.com/cmdutils/) collection.